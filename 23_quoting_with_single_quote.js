//contoh
//we can use ' so the javascript will still recognize " as a sting
var sentence = 'percobaan pake "single quote" di awal text'

//we also can use `(back tick) because now it can recognize both ' and " as string
var sentence = `tambahan kalimat di depan 'percobaan pake "single quote" di awal text'`